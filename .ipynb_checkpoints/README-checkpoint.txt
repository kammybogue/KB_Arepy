add this directory to your python module directory

alternatively add the following lines to your .bashrc file

PYTHONPATH="${PYTHONPATH}:/absolute/path/to/this/directory/"
export PYTHONPATH

and then you can just import the module with

import arepy
